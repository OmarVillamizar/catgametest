# SDD Project Configuration — catgametest
# Generated by sdd-init on 2026-06-08
schema: spec-driven

context: |
  Project: catgametest
  Repo: https://github.com/OmarVillamizar/catgametest
  Status: Active — single-file Canvas 2D endless runner game (cat-runner-game) deployed
  Stack: HTML5 + Canvas 2D (vanilla JS, zero dependencies, no build system)
  Conventions: AGENTS.md, CLAUDE.md, GEMINI.md (engram + persona + SDD orchestrator)
  CI: None (manual verification via Puppeteer harness at debug/harness.mjs)

testing:
  strict_tdd: false
  test_runner: puppeteer
  note: Puppeteer harness at debug/harness.mjs — 14 assertions across spawn, parallax, pause/restart, duck rendering, dog sizing, jump hitbox, and game-over edge cases.

quality:
  linter: null
  type_checker: null
  formatter: null

rules:
  proposal:
    - Include rollback plan for risky changes
  specs:
    - Use Given/When/Then for scenarios
    - Use RFC 2119 keywords (MUST, SHALL, SHOULD, MAY)
  design:
    - Include sequence diagrams for complex flows
    - Document architecture decisions with rationale
  tasks:
    - Group by phase, use hierarchical numbering
    - Keep tasks completable in one session
  apply:
    - Follow existing code patterns once stack is established
    - strict_tdd: false until test runner is configured
  verify:
    - Run test suite before marking complete
  archive:
    - Warn before merging destructive deltas

apply_config:
  tdd: false
  test_command: ""

verify_config:
  test_command: ""
  build_command: ""
  coverage_threshold: 0
